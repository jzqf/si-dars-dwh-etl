SHELL=/bin/sh
PATH=/usr/local/bin:/bin:/usr/bin

# Uncommenting below line will enable scheduled runs of DWH ETL process.
#0 2 * * *	su - ${app.user} -c "${app.rootDir}/${app.user}/bin/dwh-update.sh" -s /bin/bash
